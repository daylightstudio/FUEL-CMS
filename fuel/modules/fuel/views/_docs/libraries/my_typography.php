<h1>MY_Typographyd Class</h1>
<p>This extended class just contains a minor adjustment to the auto_typography to escape templating syntax so it can be properly parsed.
The escaping happens by default but if you do want to escape the quote, set a third parameter to <dfn>FALSE</dfn>.</p>